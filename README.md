For this project, I began by first creating a self-signed certificate in order to test the application. To the code I added a rest controller to the java file Sslserverappplication.java, I also included a server controller class to ensure there is a encrypted communication between two parties. To accomplish this I chose to use the encryption algorithm SHA-256 to hash the data. In order to verify the secure Communication I updated the information in the application.properties file in order to enable HTTPS with SSL keystore. To run a secondary testing I updated the pom.xml file from the older dependency check maven to the latest version which is 13.0.0. 
